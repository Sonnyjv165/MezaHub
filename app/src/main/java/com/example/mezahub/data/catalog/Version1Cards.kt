package com.example.mezahub.data.catalog

/**
 * Version 1 — PLACEHOLDER: no cards yet. Add one line per card, e.g.
 *
 *     card("1-1-001", "Species Name", StarTier.SUPERSTAR)
 *
 * Tag IDs follow 1-1-xxx (regular tags use their own IDs, e.g. "R-1-1"), and each needs a
 * matching assets/versions/v1/cries/<tagId>.wav (and optionally icons/<tagId>.jpg). Version 3's
 * catalog (Version3Cards.kt) is the reference for layout.
 */
internal val VERSION_1_CARDS = versionCards(1) {
}
