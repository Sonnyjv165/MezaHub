package com.example.mezahub.data.catalog

/**
 * Version 2 — PLACEHOLDER: no cards yet. Add one line per card, e.g.
 *
 *     card("1-2-001", "Species Name", StarTier.SUPERSTAR)
 *
 * Tag IDs follow 1-2-xxx (regular tags use their own IDs, e.g. "R-1-1"), and each needs a
 * matching assets/versions/v2/cries/<tagId>.wav (and optionally icons/<tagId>.jpg). Version 3's
 * catalog (Version3Cards.kt) is the reference for layout.
 */
internal val VERSION_2_CARDS = versionCards(2) {
}
